# Agent Notes

## Docs

- Use `npx ctx7@latest` para documentação atual de bibliotecas, frameworks, SDKs e CLIs.

## Repo Hygiene

- Mantenha `.claude/`, `.agents/` e `docs/` como contexto útil do projeto.
- Trate `.claude/settings.local.json`, `.kilocode/`, `skills-lock.json`, `.beads/`, `.runtime/`, `mayor/`, `polecats/` e `witness/` como artefatos locais.

## Shell

- Prefira comandos não interativos.
- Use `rg` para busca de arquivos e texto.
